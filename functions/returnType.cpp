#include<iostream>
#include <cmath>
using namespace std;
int sum(int x, int y){
    return x+y;
}
int main(){
    int x,y;
    cin >> x >> y;
    cout << sum(x,y) << endl; 
    cout << min(4,4) << endl;
    cout << sqrt(x);
}